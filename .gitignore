# Build output
bin/
obj/
artifacts/

# NuGet restore
*.user
.nuget/
packages/

# User secrets
appsettings.Development.json

# DocFX generated output (committed: docfx.json, docs/articles/, docs/toc.yml)
docs/api/
docs/_site/

# Doxygen generated output
docs/doxygen/

# Claude Code local settings (machine-specific, not for source control)
.claude/

# Local environment variables (loaded by direnv via .envrc; contains GH_TOKEN and any other per-developer secrets — NEVER commit)
.env
.env.*
!.env.example

# IDE
.vs/
.vscode/
*.suo
*.user
.idea/
*.DotSettings.user

# OS
.DS_Store
Thumbs.db

# Test results
TestResults/
coverage/
*.coverage
*.coveragexml

# Benchmarks output
BenchmarkDotNet.Artifacts/

# Per-agent prompt files used to bootstrap sibling-worktree Claude Code sessions
# (see AGENT-COORDINATION.md). They live in the MAIN worktree's .agent-prompts/
# directory and are gitignored so they don't pollute agent feature branches
# (the .gitignore is shared across all worktrees of this repo).
.agent-prompts/
